# SimulationSupport — user manual

```text
SimulationSupport/{include,src,tests} -> libsimulation_support.a
```

This internal module has no executable or JSON settings.

```bash
cd SimulationSupport
make
make test
```

It produces `libsimulation_support.a`. Schema names are persistent-file contracts and should not be changed as ordinary runtime parameters.
