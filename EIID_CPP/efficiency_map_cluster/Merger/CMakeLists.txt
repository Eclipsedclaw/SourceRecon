cmake_minimum_required(VERSION 3.20)
if(NOT DEFINED EFF_PROJECT_ROOT)
    project(EfficiencyMerger LANGUAGES CXX)
    include(CTest)
    set(EFF_BUILD_SIMULATION OFF CACHE BOOL "" FORCE)
    set(EFF_BUILD_MERGER ON CACHE BOOL "" FORCE)
    add_subdirectory(.. suite)
    return()
endif()
add_executable(efficiency_merge src/main.cpp src/CountMerger.cpp)
target_include_directories(efficiency_merge PRIVATE include)
target_link_libraries(efficiency_merge PRIVATE eff_root_io)
target_compile_options(efficiency_merge PRIVATE -Wall -Wextra -Wpedantic)
